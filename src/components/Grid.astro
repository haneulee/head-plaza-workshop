---
const { cols = "2", class: className } = Astro.props;

const colClass = cols === "2" ? "md:grid-cols-2" : "md:grid-cols-4";
---

<div class:list={["grid grid-cols-1", colClass, className]}>
  <slot />
</div>
